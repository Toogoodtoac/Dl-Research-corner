# Backend Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=true

# OpenMP Settings (fixes library conflicts on macOS)
KMP_DUPLICATE_LIB_OK=TRUE

# CORS Settings
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:8080"]

# Model Paths (relative to backend directory)
SUPPORT_MODELS_DIR=../support_models
CLIP_MODEL_NAME=ViT-B/32
LONGCLIP_CHECKPOINT=../support_models/Long-CLIP/checkpoints/longclip-B.pt
CLIP2VIDEO_CHECKPOINT=../support_models/CLIP2Video/checkpoints

# FAISS Index Paths - updated to use new structure
FAISS_CLIP_PATH=../dict/faiss_clip_vitb32.bin
FAISS_LONGCLIP_PATH=../dict/faiss_longclip.bin
FAISS_CLIP2VIDEO_PATH=../dict/faiss_clip2video.bin
ID2IMG_JSON_PATH=../dict/id2img.json

# Data Paths - updated to use Google Drive or local fallback
DATA_ROOT=../data
FEATURES_DIR=../features
RAW_DATA_DIR=../data

# Indexing paths
INDEXES_DIR=../features
HDF5_FEATURES_DIR=../features
FAISS_INDEXES_DIR=../features
LUCENE_INDEXES_DIR=../data/lucene
SAMPLE_DATA_DIR=../examples/sample_data

# Cache Settings
REDIS_URL=redis://localhost:6379
CACHE_TTL=3600

# Model Loading
LAZY_LOAD_MODELS=true
MODEL_DEVICE=auto

# Search Settings
DEFAULT_SEARCH_LIMIT=20
MAX_SEARCH_LIMIT=100

# OCR Settings
OCR_MODEL_PATH=../support_models/OCR

# ASR Settings
ASR_MODEL_PATH=../support_models/ASR
